# Tower Battle Intel — v4.11z52w29

## Command Deck Panel Hierarchy Rebuild

- Command Deck hero is now workflow-only and no longer mirrors Active Data.
- Hero text now reflects the rebuilt spine: paste Battle Reports, validate, save raw source safely, manage in History, and load clean runs into Dashboard.
- Four-step strip now reads Paste / Validate / Save / Manage.
- `Next Steps` has been replaced by `Report Flow`, with dynamic `After Save` wording when a report was just saved.
- Compare route is hidden from Command Deck while Compare remains parked.
- `Current Data` is now `Active Data` and owns the current state summary.
- `System Readiness / Control Health` is now `Intake Health` and reports the real intake spine status.
- Command Rules now point global/dangerous data management toward the future Settings rebuild.
- Dashboard, Header, Command Deck raw save path, rebuilt History, Stats/Edit modals, raw archive, and mobile remain protected.

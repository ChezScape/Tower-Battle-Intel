# Changelog — v4.11z52w14

- Command Deck is now the default app starting workspace.
- Desktop Command Deck now uses the active `commandDeckView` workspace instead of the parked shell.
- Command Deck buttons are rewired through `src/ui/events/commandDeckEvents.js`.
- Command Deck supports Validate, Save Report, Save + Dashboard, Clear Input, Import History, and Export History.
- Command Deck textarea draft input and build-style changes are now actively handled.
- Architecture rulebook updated with Command Deck active-owner entries and Dashboard ownership rules.

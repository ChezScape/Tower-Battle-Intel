# Changelog v4.11z52w33 — History Stats Modal Clarity Polish

- Updated visible/display build to `v4.11z52w33` while keeping protected core `v4.11z52`.
- Polished the rebuilt History Stats modal without changing the Summary / Sections / Raw Source tab structure.
- Changed user-facing `Raw Archive Verified` wording to `Raw Source Verified` and removed the duplicate Source tile from the modal trust row.
- Renamed `Quality` to `Performance Score` so short/tournament runs do not look like bad report data.
- Moved Set Run A / Set Run B / Edit Metadata into a run-action bar near the modal title.
- Kept Copy JSON / Download JSON grouped at the bottom as Data actions.
- Added previous-run comparison context and warns when the delta compares different run types, such as Normal → Tournament.
- Removed low-value `Archived in scope` from Stats modal Library Context and replaced it with run-type context.
- Added a focused Stats modal polish test.
- Dashboard, Header, Command Deck raw save path, rebuilt History hub, Edit modal, raw archive spine, and mobile remain protected.
